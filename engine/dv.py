#!/usr/bin/python

from sys import stderr

while True:
    for i in xrange(11):
        #x, y = map(int, raw_input())
        x = int(raw_input())
        y = int(raw_input())
        print x, y
   	s = int(raw_input())
